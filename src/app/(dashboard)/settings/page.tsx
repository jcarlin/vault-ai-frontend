// Settings content is rendered directly by DashboardLayout (which owns the
// settingsCategory state). This file exists only so Next.js recognizes the
// /settings route.
export default function SettingsRoute() {
  return null;
}
